/**
 * toString():
 * - This method returns a string representing the specified array and its 
 *   elements.
*/

/**
 * Q. Ek array hai, aur uss array ko string me convert karo.
*/
const arr = ['apple', 'banana', 'cherry'];
const str = arr.toString();
console.log(str);  // Output: 'apple, banana, cherry'