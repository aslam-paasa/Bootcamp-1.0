/**
 * utils.mjs — named exports
 * Both functions are exported so any file can import them by name.
 */

export function add(a, b) {
  return a + b;
}

export function subtract(a, b) {
  return a - b;
}
