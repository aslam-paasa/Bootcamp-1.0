/**
 * Write an ES6 fn that takes an array of strings and returns an array
 * with all the strings capitalized using the map method.
*/

const myArray = ['apple', 'banana', 'cherry']
console.log(capitalizeArray(myArray)); // Output: ["APPLE", "BANANA", "CHERRY"]
