/**
 * Write an ES6 fn that takes an array of strings and returns a single
 * string that is the concentration of all the strings using the
 * reduce fn.
*/

const strings = ['Hello', ' ', 'world', '!']

// Your code here

console.log(concatenateStrings(strings))
// Output: "Hello world!"