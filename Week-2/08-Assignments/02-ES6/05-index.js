/**
 * Write an ES6 fn that takes an array of numbers and returns the sum
 * of all the even numbers. Use the nullish coalescing operator to
 * handle undefined or null values.
*/

console.log(sumOfEvenNumbers([1, 2, 3, 4, 5, 6])) // 12
console.log(sumOfEvenNumbers([1, 3, 5])) // 0