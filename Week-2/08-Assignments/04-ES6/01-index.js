/**
 * Write an ES6 fn that accepts an array of integers and returns the
 * maximum element in the array. Avoid using in-built methods.
*/

let array = [4, 78, 8, 3, 6, 0, 12, 34]
console.log(getMaxElement(array)) // 78
