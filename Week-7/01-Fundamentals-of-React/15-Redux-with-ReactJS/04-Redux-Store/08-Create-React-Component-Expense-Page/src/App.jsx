/**
 * Challenge-4.2: Create React Component - expense page
 * 1. Create a new file named Expense.jsx
 * 2. Inside the Expense component:
 *    a. Use useDispatch to get the dispatch function.
 *    b. Use useSelector to access the expenses data from the Redux Store
 *    c. Calculate the totalExpenses by reducing the expenses array.
 *    d. Implement a useEffect to dispatch the fetchExpenses action
 *       when the component mounts.
 *    e. Render the expense data as a list (e.g, a <ul> with <li> ele)
 *    f. Display the totalExpenses as a summary. 
*/