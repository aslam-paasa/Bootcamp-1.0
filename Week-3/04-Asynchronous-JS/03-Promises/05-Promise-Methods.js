/**
 * Promise Methods:
 * a. Promise.all() : Runs multiple promises in parallel and returns results
 *                    when all succeed.
 * 
 * b. Promise.race(): Returns the result of the first settled promise.
 *                    (Whether resolved or rejected)
 * 
 * c. Promise.any() : Returns the first fulfilled(resolved) promise, ignoring
 *                    rejected ones.
 * 
 * d. Promise.allSettled() : Returns the results of all promises, regardless
 *                           of whether they are fulfilled or rejected.
*/
