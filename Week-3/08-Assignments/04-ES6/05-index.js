/**
 * Write an ES6 fn that takes an array of strings and returns a new
 * array with each string capitalized.
*/

console.log(capitalizeWords(['eat', 'sleep', 'code', 'repeat']));
// ["EAT", "SLEEP", "CODE", "REPEAT"]
