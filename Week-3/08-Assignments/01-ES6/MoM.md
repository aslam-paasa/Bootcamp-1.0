# Important Instructions:
1. Avoid using in-built methods in JS. You can make use of basic methods such as length, toLowerCase(), toUpperCase(), push() if needed.
2. Make use of for-loops and if-else statements wherever needed.