/**
 * NOT IN Operator:
 * > Exclude multiple values from results. 
 * > Opposite of IN.
 * 
 * > NOT IN returns rows where the column does NOT match any of the 
 *   values. 
 * > This finds users from countries other than USA and Canada.
 * > Hint: Use WHERE column_name NOT IN (value1, value2)
*/

/**
 * Q. Select name and country from users where country is NOT in 
 *    ('USA', 'Canada').
*/