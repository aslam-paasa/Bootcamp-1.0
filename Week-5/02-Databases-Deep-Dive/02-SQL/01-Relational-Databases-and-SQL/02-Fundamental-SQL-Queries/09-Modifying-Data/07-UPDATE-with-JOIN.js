/**
 * UPDATE with JOIN:
 * > Update a table based on data from another table. 
 * > Combine UPDATE with JOIN for complex updates.
 * 
 * > IMPORTANT: 
 *   Reset the database before this lesson! 
 *   UPDATE...FROM allows you to join tables and update based on related
 *   data.
*/

/**
 * Q. Click "Reset DB" before starting! Update all orders from users 
 *    in 'USA' - set their status to 'priority'.
 * 
 * > Hint: Use UPDATE table1 SET column = value FROM table2 WHERE table1.id = table2.id AND condition
*/