/**
 * Why do we write tests?
 * - To save time doing the manual labor
 * - Coverage ensures that all code paths are tested
 *   - It saves us from making mistakes
 * - If tests run on CI, then no bad build will be delivered to 
 *   end-user. 
 * - It helps us refactor or add features with confidence.
 * - Manually testing, you would test less. But automated test can run
 *   on every change and make sure things work.
 * - Tests serve as the best documentation as you don't have to work
 *   extra to keep them up to date. In summary, they don't go stale.
 * - You'll end up writing better code. Mostly when you do TDD[Test
 *   Driven Development].
 * - TDZ means you write test first, then you make that test fail and
 *   then refactor the code to make it beautiful and again the test
 *   should pass.
 *   => RGR(Red Green Refactor)
 *      a. Modularity: Small-Small parts of the program, so it is easier
 *         to test the part than the whole system.
 *      b. Pure Functions
*/

