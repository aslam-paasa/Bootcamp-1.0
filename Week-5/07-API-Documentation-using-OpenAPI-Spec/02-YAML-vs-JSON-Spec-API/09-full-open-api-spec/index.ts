/**
 * Creating an OpenAPI Spec for Todo API:
 * > Goal   : Create a full OpenAPI spec for Todo API
 * > Outcome: Understand how to create a full OpenAPI spec for Todo API
 * 
 * Features of the Todo API:
 * 1. YAML is indentation-sensitive - spaces matter.
 *    No {} or [] like JSON.
 * 2. $ref - reuses schema definitions (User, Todo).
 * 3. security - applies authentication (JWT in this case).
 * 4. parameters - define path or query inputs.
 * 5. requestBody - data sent by the clients.
 * 6. responses - possible response codes and structutres.
 * 7. YAML is more readable than JSON because it avoids brackets, quotes,
 *    and commas.
*/