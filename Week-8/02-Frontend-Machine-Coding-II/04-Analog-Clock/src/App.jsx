/**
 * Analog Clock:
 * Create a widget that renders the current time within an analog clock 
 * display, where the clock hands move and update like a real clock.
*/

import Clock from './Clock';
import './App.css';

export default function App() {
  return (
    <div className="wrapper">
      <Clock />
    </div>
  );
}

