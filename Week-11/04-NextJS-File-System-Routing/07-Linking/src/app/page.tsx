/**
 * 1. Inside app folder, we define our pages. 
 * 2. Inside component folder, we define our react component.
*/

import Navbar from "@/components/Navbar"

const Home = () => {
  return (
    <div>
      <Navbar />
    </div>
  )
}

export default Home