/**
 * Complex Interface Practice:
 * Q. Create an interface Car with the following properties:
 *    - make(string)
 *    - model(string)
 *    - year(number)
 *    - isElectric(boolean)
 *    - features(array of strings)
*/

interface Car {
    make: string
    model: string
    year: number
    isElectric: boolean
    features: string[]
}
