/**
 * Q. Create an object student with properties name(string), age(number)
 *    and isEnrolled(boolean)
 * */ 

let student: {
    name: string,
    age: number,
    isEnrolled: boolean
} = {
    name: 'Bob',
    age: 20,
    isEnrolled: true,
};