/**
 * Q. Define a type quizChecker for a fn that takes a QuizQuestion
 *    (of type quizQuestion) and a userAnswer(number), and returns a
 *    boolean indicating whether the user's answer is correct.
*/

// type quizChecker = (quizQuestion: quizQuestion, userAnswer: number) => boolean

// const checkQuizAnswer: quizChecker = (quizQuestion, userAnswer) => {
//  return userAnswer === quizQuestion.correctOption
// }