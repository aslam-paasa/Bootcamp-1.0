/**
 * Function with two inputs:
 * Q. Create a function named addNumbers that takes two parameters
 *    a(number) and b(number), adds them together, and console the
 *    result.
 * */ 

function addNumbers(a: number, b: number): void {
    const sum = a + b;
    console.log(`Sum: ${sum}`);
}