/**
 * Functions:
 * function functionName(parameter: parameterType): functionReturnType {}
 * */ 

/**
 * Q. Define a function named greet that takes a parameter name (String)
 *    and consoles a greeting message like "Hello, [Name]!".
 * */ 

function greet(name: string): void {
    console.log(`Hello, ${name}!`);
}