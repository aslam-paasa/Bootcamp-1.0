/**
 * Create a login route:
 * Q1. Create a POST route "/login" that accepts a username and password
 *     in the request body.
 * Q2. Assume that the username and password are correct for this username.
 * Q3. Generate a JWT token with a user ID and return it in the response
 *     along with the username.
 * */ 