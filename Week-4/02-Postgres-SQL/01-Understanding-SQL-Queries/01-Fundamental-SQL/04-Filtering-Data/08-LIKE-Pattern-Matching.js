/**
 * LIKE Pattern Matching:
 * > Search for patterns in text. 
 * > Use % for zero or more characters, _ for exactly one character.
 * 
 * > LIKE uses wildcards: % matches any characters, _ matches one 
 *   character. 
 * > 'A%' finds names starting with A.
 * 
 * > Hint: Use WHERE column_name LIKE 'A%' 
 *         (% means any characters after A)
*/

/**
 * Q. Select name and email from users where name starts with 'A' 
 *    (case-sensitive).
*/