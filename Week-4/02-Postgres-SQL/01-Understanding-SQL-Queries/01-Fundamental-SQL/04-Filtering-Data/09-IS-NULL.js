/**
 * IS NULL:
 * > Find rows with NULL (missing) values. 
 * > Some users don't have phone numbers in our database.
 * 
 * > NULL means "no value" or "unknown". 
 * > Use IS NULL to find missing data. 
 * > Note: You cannot use = NULL.
 * 
 * > Hint: Use WHERE column_name IS NULL (not = NULL)
*/

/**
 * Q. Select name and phone from users where phone is NULL.
*/