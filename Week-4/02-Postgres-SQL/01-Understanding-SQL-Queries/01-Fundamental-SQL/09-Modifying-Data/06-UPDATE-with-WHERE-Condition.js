/**
 * UPDATE with WHERE Condition:
 * > Update multiple rows that match a condition. 
 * > Powerful for batch updates.
 * 
 * > IMPORTANT: 
 *   Reset the database before this lesson! 
 *   You can use expressions in SET. 
 *   WHERE filters which rows to update. 
 *   Without WHERE, ALL rows are updated!
*/

/**
 * Q. Click "Reset DB" before starting! 
 *    Increase the price of all products in category_id=1 (Electronics) 
 *    by 50. 
 * 
 * > Hint: price = price + 50.
 * > Hint: Use UPDATE table SET column = column + value WHERE condition
*/