/**
 * INSERT with Specific Columns:
 * > You can omit columns that are optional or have default values. 
 * > Useful when you don't have all data.
 * 
 * > IMPORTANT: 
 *   Reset the database before this lesson! 
 *   When columns allow NULL or have defaults, you can omit them. 
 *   Phone, age, country, and city can be NULL.
*/

/**
 * Q. Click "Reset DB" before starting! Insert a new user with only: 
 *    - name='Jane Doe' and 
 *    - email='jane@example.com' (omit other columns).
 * 
 * > Hint: Use INSERT INTO table (column1, column2) VALUES (value1, value2)
*/