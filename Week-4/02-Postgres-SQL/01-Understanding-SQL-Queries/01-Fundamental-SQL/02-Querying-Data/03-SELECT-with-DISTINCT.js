/**
 * SELECT with DISTINCT:
 * > Remove duplicate values from results. 
 * > Our users are from different countries - let's see which unique 
 *   countries they represent.
 * 
 * > DISTINCT removes duplicate rows from results. 
 * > Since multiple users can be from the same country, this shows only
 *   unique values.
 * > Syntax: Use SELECT DISTINCT column_name FROM table_name
*/

/**
 * Q. Select all unique countries from the users table (no duplicates).
*/