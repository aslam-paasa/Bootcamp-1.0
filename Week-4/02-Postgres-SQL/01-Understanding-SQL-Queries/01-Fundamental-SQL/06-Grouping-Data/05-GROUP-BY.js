/**
 * GROUP BY:
 * > Group rows with the same values and apply aggregation to each group. 
 * > Our users are distributed across 4 countries.
 * 
 * > GROUP BY creates groups for each unique value, then applies COUNT 
 *   to each group. 
 * > This shows user distribution by country.
 * 
 * > Hint: Use SELECT column_name, COUNT(*) FROM table_name GROUP BY column_name
*/

/**
 * Q. Count the number of users in each country. 
 *    Show country name and count.
*/