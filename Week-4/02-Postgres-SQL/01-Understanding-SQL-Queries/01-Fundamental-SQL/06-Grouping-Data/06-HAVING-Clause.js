/**
 * HAVING Clause:
 * > Filter groups after aggregation. 
 * > WHERE filters rows before grouping, HAVING filters groups after.
 * 
 * > HAVING filters the grouped results. 
 * > It's like WHERE, but for groups. 
 * > Only countries with 3+ users will appear.
 * 
 * > Hint: Use GROUP BY column_name HAVING COUNT(*) > value
*/

/**
 * Q. Show countries that have more than 3 users. 
 *    Display country and user count.
*/